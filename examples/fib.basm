# first two fibonacci numbers
        jmp 1
        nop
        nop
        nop
        push 0
        push 1
loop:   dup 1 # the loop
        dup 1
        plus
        dump
        jmp loop   # repeat
        hlt